///Importing 'size_config_web' will only give you access to extension methods such as `.w` ,`.h` and `.sp`.
///[Suggestion] Add this dependency only to the main method to initialize `initDevSize` method.
library size_config;

//From Folder responsiveness
export 'src/exports/responsiveness/size_config.dart';
